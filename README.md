# Text Detection with EasyOCR

This project demonstrates simple text detection on images using EasyOCR, OpenCV, and Matplotlib. It loads an image, runs OCR to detect text, draws bounding boxes around detected regions, and visualizes the result.

## Features

- Text detection from images.
- Adding boxes and labels on detected text regions.
- Visualizing results using Matplotlib.
